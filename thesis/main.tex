% masterthesis_ceng for Computer Science
% masterthesis_ceng for Computer Engineering
\documentclass{masterthesis}

% Macros
\newcommand{\authornote}[2]{
    \fbox{\bfseries\sffamily\scriptsize#1}
    {\small\textit{\color{red} #2}}
}

\newcommand\ernote[1]{\authornote{Enrico}{#1}}
\newcommand\glnote[1]{\authornote{MiloG}{#1}}
\newcommand\grnote[1]{\authornote{MiloG}{#1}}


% the configuration is in the preamble.tex file, read it and modify it to change
% the document appearance and included packages
\input{preamble}

% by default, we use the biblatex package to manage the bibliography
% if you prefer to use bibtex, comment the following line and uncomment the
% relevant lines at the end of the document
\addbibresource{bib.bib}

\begin{document}

\title{Massively parallel real time simulation of primary radar systems}

\author{Milo Galli}

\advisor{Enrico Russo}

\examiner{Giovanni Lagorio}

% comment /maketitle and /tableofcontents to speed up the compilation time
\maketitle

\begin{abstract}
	In this work we will present a simulator for primary radar systems based on
	the shooting and bouncing ray method, capable of running efficiently on
	commodity GPU hardware. In particular, our implementation, based on the
	Vulkan API, leverages the ray accelerators present in the recent
	generations of GPUs to gain real-time performances. The simulator that will
	be discussed in this work is able to simulate monostatic and bistatic
	radars, with a fully customizable antenna radiation pattern, in a 3D
	environment with physically based materials. Furthermore, our simulator
	supports an environment containing moving objects whose speed is accounted
	in the simulation of Doppler shifts. We will then compare our
	implementation against a selected existing state-of-the-art solution
	showing that we get comparable results while achieving significant better
	performances.
\end{abstract}

\tableofcontents

\include{introduction}
% chi siamo e cosa vogliamo dal mondo
% che problema risolviamo
% simulare radar primari è una cosa importante in molti settori
% ci hanno provato tante persone, ed è parte di molte attività
% in particolare ci interessa il training dei marittimi, il supporto alla progettazione di nuovi dispositivi, lo sviluppo di algoritmi nuovi e sistemi
% al momento le prove si fanno in modo barbaro, costoso, e poco rappresentativo
% non si trovano dataset quindi rip machine learner, anomaly detection, yolovsalcazzo, e quindi i machine learner non possono fare il loro utilissimo lavoro
% i simulatori da addestramento hanno bisogno di questa cosa e tanto meglio la si fa tanto più bello è il training, inoltre sarebbe bello avere parametri
% i progettisti al momento devono sucarsi procedure molto lente e vincolate a software proprietari, costosi, ecc. ecc.
% purtroppo questi skill issue e non sanno che esiste questa nuova cosa che useremo nella tesi che permette di sfruttare l'hw molto meglio e fare queste simulazioni in modo decente
% in questa tesi mostreremo quindi un overview del problema, delle sue componenti e proveremo che il nostro sistema va, accuratamente e velocemente (rispetto agli altri)

\include{background}
% radar primari e loro applicazioni
% non compromettenti:
% - radar di navigazione
% - radar automobilistici
% - cognitive radar
% - radar primari per controllo traffico aereo che vengono utili quando il radar secondario si rompe
% modo di funzionamento (lancio -> rimbalza -> ricevo)
% variabili significative (quello che misurano i radar / quello che serve per simularli)
% - attenuazione (nella simulazione serve perchè segnali troppo deboli si confondono con il rumore)
% - doppler (la usano le macchine per capire il closure rate)
% - tempo di attraversamento
% fenomeni significativi (la realtà che fa)
% - riflessione
% - rifrazione
% - diffrazione
% - multipath
% - fresnallate, lambertate, eikonalate, snellate, ...
% - dopplerata
% - antenne non sono isotropiche (pattern / gain)
% - interferenza mutuale
% tecniche a lancio raggi per simulazione della propagazione
% - assunzione a hf
% - in generale come va (generazione, propagazione, ricezione, materiali, ...)
% accelerazione hardware al lancio raggi
% - geometrie e triangoli (il mondo per un computer è fatto di triangoli)
% - AABB / spatial indexing
% API vulkan
% - accesso all'hardware
% - vendor neutralness e fanculo directx/cuda/optix/rocm/rays/...
% - compatibile bella e brava
% - Ray acceleration e eventuali altre terminologie (pipeline / shader / come è fatta ray tracing pipeline / memory allocation)

\include{related}
% lanciare raggi per simulare radar s'è già fatto
% - 10+ lavori descritti brevemente rispetto al introduction e background detto prima
% loro lo hanno fatto
% - slower
% - vendor lock in
% - sovereignity
% - scenari più piccoli / più fissi
% - non si è capito bene cazzo fanno
% - costano
% per cui noi ci siamo sentiti di migliorare lo stato dell'arte
% trovare N feature caratteristiche (che noi abbiamo) e fare i pallini
% pippa su come i motori di gioco non siano sufficienti e su come usare solo il depth buffer sia un'idea del cazzo

\include{method}
% nel complesso il sistema fa questo
% tutti i sottopezzi divertenti
% - generare raggi
% - pattern
% - rimbalzone
% - il ray payload magico
% - assemblaggio alla fine
% - input e output (generare e muovere roba)
% personalizzazione per casi d'uso
% - le levette a disposizione per rendere tuo il sistema
% - simulatore come library
%   abbiamo definito che per integrarci fuori dobbiamo esporre delle primitive per caricare/scaricare mesh, ruotarle, posizionarle, le caratterizzi, tocchi i tunable, scegli come farti girare l'output
%   questa cosa ci rende flessibili e belli perchè puoi agganciarti dall'esterno

\include{results}
% caso d'uso (radar di navigazione marittima)
% - il patternone, ...
% setup sperimentale (pc, os, scena) numbers
% risultati qualitativi (va, sembra ok, si vede fenomeno X)
% - foto, range doppler charts
% risultati quantitativi (rays/s, cpu, ram, ...)
% comparison con SOTA
% - rispetto a matlab andiamo più veloci e ci viene uguale/più bello
% ? se qualcuno direttamente comparabili siamo più veloci/belli
% ?? in generale rispetto a X, Y, Z performance diverse

\include{conclusion}
% qua avete visto che c'era un problema XXX
% abbiamo presentato una metodologia che lo risolve
% i nostri esperimenti hanno fatto vedere che funziona
% grazie e buonasera

% Bibliography using biber; comment this and uncomment the following lines to use bibtex instead
\printbibliography
% \bibliographystyle{alphaurl}
% \bibliography{bib}

\end{document}

